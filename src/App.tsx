import React from 'react';

const App: React.FC = () => (
  <div>
    <h2>React App</h2>
    <p>React App in Typescript including linting with eslint</p>
    <sub>
      <code>npx create-react-app --use-npm --typescript</code>
    </sub>
  </div>
);

export default App;
