import React, { Fragment } from 'react';

const Intro = () => (
  <Fragment>
    <h1> Welcome welcome welcome</h1>
    <p>Search for a GitHub user by username here</p>
  </Fragment>
);

export default Intro;
